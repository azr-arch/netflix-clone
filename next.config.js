/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["mango.blender.org", "uhdtv.io", "upload.wikimedia.org", "download.blender.org"],
    },
};

module.exports = nextConfig;
